version: '3.8'

services:
  pulse-engine:
    build: .
    container_name: muzebiz-pulse
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
