const fetch = require('node-fetch');
const ticker = 'MULN';
const apiKey = 'YOUR_KEY_HERE'; // I'll need to get the key from the env or just use the existing service.
// Actually, I can just use curl to hit the local edge function if it's running.
