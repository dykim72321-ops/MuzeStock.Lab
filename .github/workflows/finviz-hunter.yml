name: Finviz Hunter Bot

on:
  schedule:
    # 매일 오전 8시(KST) = UTC 23:00 (전날)
    - cron: "0 23 * * *"
  workflow_dispatch: # 수동 실행 허용

jobs:
  hunt:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: |
          npm ci
          npx playwright install chromium
          npx playwright install-deps # Fix 2: Install system dependencies for Linux

      - name: Check Secrets (Debug)
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          if [ -z "$VITE_SUPABASE_URL" ]; then echo "❌ VITE_SUPABASE_URL is missing"; else echo "✅ VITE_SUPABASE_URL is set"; fi
          if [ -z "$SUPABASE_SERVICE_ROLE_KEY" ]; then echo "❌ SUPABASE_SERVICE_ROLE_KEY is missing"; else echo "✅ SUPABASE_SERVICE_ROLE_KEY is set"; fi

      - name: Run Finviz Hunter
        id: run_scraper
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: node scripts/daily_scraper.js

      - name: Summary
        run: echo "🎯 Finviz Hunter Bot 완료! Supabase daily_discovery 테이블이 업데이트되었습니다."
