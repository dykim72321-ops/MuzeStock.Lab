name: Finviz Hunter Bot (Python)

on:
  schedule:
    # 매일 오전 8시(KST) = UTC 23:00 (전날)
    - cron: "0 23 * * *"
  workflow_dispatch: # 수동 실행 허용

jobs:
  hunt:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: python_engine

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          cache: "pip"
          cache-dependency-path: python_engine/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install --with-deps chromium

      - name: Run Unified Hunter
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || secrets.VITE_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY || secrets.SUPABASE_SERVICE_ROLE_KEY }}
          VITE_FINNHUB_API_KEY: ${{ secrets.VITE_FINNHUB_API_KEY }}
          
        run: python scraper.py

      - name: Summary
        run: echo "🎯 Unified Python Hunter Bot 완료! 데이터 수집 및 AI 분석이 성공적으로 저장되었습니다."
